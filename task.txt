ROLE: Lead Performance Architect.
MISSION: Radical optimization of the system's "Complexity Router" and AI Provider logic.

CORE OBJECTIVES (Ranked):
1. ACCURACY (Non-negotiable constraint).
2. REQUEST ECONOMY (Minimize the sheer number of HTTP requests. One-shot perfection is the goal. Eliminate retries/chains).
3. SPEED (Reduce end-to-end latency).
4. ZERO COST (Strict adherence to FREE models with High Rate Limits).

CONTEXT:
We currently use a tiered system (`docs\PROVIDERS.md`). It may be inefficient—sending necessary fallback requests, using verbose prompts, or failing to utilize the full power of Tier 1 models.

YOUR AUTONOMY:
You are NOT restricted to the current logic. You are free to:
- Rewrite the routing algorithms to be "One-Shot" aggressive.
- Change the model hierarchy (provided they are Free/High-Limit).
- Aggressively condense prompts to save tokens.
- Dig into specific OpenRouter model IDs to find the hidden gems (fastest/freest).
- Implement "Fail-Fast" logic: If a request is likely to fail (bad input), don't send it at all.
- Implement "Parallel Speculation" if it saves time without costing money (e.g. firing two free models at once).

MANDATORY PROTOCOL (The Scientific Method):
1. ESTABLISH BASELINE: Do not change code until you have a `benchmark.py` running.
   - It MUST measure: [Total Requests Sent], [Avg Latency], [Token Usage], [Accuracy %].
2. FORMULATE A PLAN: Analyze the codebase. Identify where we are "leaking" requests (e.g., unnecessary pre-checks, eager fallbacks, redundant calls).
3. ITERATE:
   - Implement a change.
   - Run `benchmark.py`.
   - IF (Fewer Requests/Faster AND Same Accuracy) -> COMMIT.
   - IF (Accuracy drops) -> REVERT immediately.

CONSTRAINTS:
- Use *only* models that are FREE and have lenient Rate Limits.
- If using OpenRouter, select SPECIFIC free/high-limit model IDs. Do not use generic routers.

GO:
Read the docs, explore the code, build your benchmark, and start optimizing.
