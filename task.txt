ROLE: Reliability Engineer / QA Lead.
MISSION: STRESS TEST the Optimized Extraction Pipeline to identify breaking points, memory leaks, and long-run rate limits.

CONTEXT:
We have optimized the pipeline to ~0.6s latency for small batches (5 msgs) with 100% accuracy. We need to verify if this performance holds for LARGE datasets (500+ messages) and long-running sessions.

OBJECTIVES:
1. CREATE MEGA-DATASET:
   - Generate or synthesize a `benchmark/dataset/stress_test_500.json` file.
   - Use a mix of "Simple" (Rule-based) and "Complex" (AI-required) messages to simulate real traffic (Ratio: 80% Simple, 20% Complex).
   
2. EXECUTE STRESS TEST:
   - Create `benchmark/stress_test.py`.
   - Run the pipeline against the 500 messages with `CONCURRENCY=4`.
   - MONITOR:
     - Memory Usage (Does it creep up?)
     - Rate Limits (Do we hit 429s after minute 5?)
     - Latency Drift (Does 0.6s become 2.0s after 100 messages?)

3. OPTIMIZE FOR ENDURANCE:
   - If Rate Limits hit: Implement "Smart Throttle" (dynamic sleep) or "Backoff" logic.
   - If Memory leaks: Add garbage collection or stream processing.
   - If Latency drifts: Investigate Provider fatigue.

PROTOCOL:
1. Generate the dataset.
2. Run the stress test.
3. Analyze logs.
4. Fix ONE major bottleneck found.
5. Report findings.

CONSTRAINTS:
- Do not spend real money (Keep to Free Tier models).
- Maintain the "Batch Size 1" safety constraint.

GO.
